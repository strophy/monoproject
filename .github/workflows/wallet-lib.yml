name: Wallet Library Tests

on:
  workflow_dispatch:
  schedule:
    - cron: '41 */160 * * *'
  release:
    types:
      - published
  pull_request:
    branches:
      - master
      - v[0-9]+\.[0-9]+-dev
    paths:
      - packages/wallet-lib/**
      - .github/workflows/wallet-lib.yml

jobs:
  wallet-lib-tests:
    name: Run Wallet Library tests
    uses: strophy/monoproject/.github/workflows/test.yml@master
    with:
      package-directory: packages/wallet-lib
      run-test-suite: true
