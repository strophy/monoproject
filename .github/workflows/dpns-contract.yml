name: DPNS Contract Tests

on:
  workflow_dispatch:
  schedule:
    - cron: '41 */160 * * *'
  release:
    types:
      - published
  pull_request:
    branches:
      - master
      - v[0-9]+\.[0-9]+-dev
    paths:
      - packages/dpns-contract/**
      - .github/workflows/dpns-contract.yml

jobs:
  dpns-contract-tests:
    name: Run DPNS Contract tests
    uses: strophy/monoproject/.github/workflows/test.yml@a5b6eb56d1d01078aa817b100719fccd708c9a19
    with:
      package-directory: packages/dpns-contract
